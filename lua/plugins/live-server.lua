return {
  "wolandark/vim-live-server",
  cmd = { "StartBrowserSync", "KillBrowserSync" },
  ft = { "html", "css", "javascript", "typescript" },
  config = function()
    -- Optional: Add any custom configuration here
    -- The plugin works out of the box with default settings
  end,
}